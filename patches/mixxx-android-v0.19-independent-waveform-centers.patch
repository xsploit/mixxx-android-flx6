diff --git a/packaging/android/AndroidManifest.xml b/packaging/android/AndroidManifest.xml
index 50f593d..58efe0a 100644
--- a/packaging/android/AndroidManifest.xml
+++ b/packaging/android/AndroidManifest.xml
@@ -2,8 +2,8 @@
 <manifest xmlns:android="http://schemas.android.com/apk/res/android"
     package="org.mixxx.flx6standalone"
     android:installLocation="auto"
-    android:versionCode="18"
-    android:versionName="0.18.0-edit-locked-draw-scale">
+    android:versionCode="19"
+    android:versionName="0.19.0-independent-waveform-centers">
     <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"  />
     <uses-permission android:name="android.permission.INTERNET"  />
     <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>
@@ -22,7 +22,7 @@
         android:name="org.qtproject.qt.android.bindings.QtApplication"
         android:hardwareAccelerated="true"
         android:icon="@mipmap/ic_launcher"
-        android:label="Mixxx FLX6 v0.18"
+        android:label="Mixxx FLX6 v0.19"
         android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
         android:requestLegacyExternalStorage="true"
         android:allowBackup="true"
diff --git a/res/qml/WaveformDisplay.qml b/res/qml/WaveformDisplay.qml
index 04a4b68..044297d 100644
--- a/res/qml/WaveformDisplay.qml
+++ b/res/qml/WaveformDisplay.qml
@@ -15,15 +15,20 @@ Item {
 
     required property string group
     property bool splitStemTracks: false
+    property real verticalDrawPosition: 0.5
     property real verticalDrawScale: 1.0
+    readonly property real verticalDrawCenter: nativeWaveform.y + nativeWaveform.height / 2
     readonly property string zoomGroup: Mixxx.Config.waveformZoomSynchronization ? "[Channel1]" : group
 
     MixxxControls.WaveformDisplay {
-        anchors.centerIn: parent
+        id: nativeWaveform
+
         backgroundColor: "transparent"
         group: root.group
         height: parent.height * root.verticalDrawScale
         width: parent.width
+        x: 0
+        y: (parent.height - height) * Math.max(0, Math.min(1, root.verticalDrawPosition))
         zoom: zoomControl.value
 
         Behavior on zoom {
diff --git a/res/qml/main.qml b/res/qml/main.qml
index 5ff288d..ad210ab 100644
--- a/res/qml/main.qml
+++ b/res/qml/main.qml
@@ -23,6 +23,8 @@ ApplicationWindow {
     property alias showEffects: showEffectsButton.checked
     property alias showSamplers: showSamplersButton.checked
     property bool toolbarExpanded: false
+    property real deckADrawPosition: 0.5
+    property real deckBDrawPosition: 0.5
     property real waveformDrawScale: 1.0
     property real waveformStackPosition: 1.0
     readonly property int waveformTopGuard: compactScreen ? 5 : 0
@@ -417,6 +419,7 @@ ApplicationWindow {
                             sourceComponent: Component {
                                 Skin.WaveformDisplay {
                                     group: deck3waveform.group
+                                    verticalDrawPosition: root.deckADrawPosition
                                     verticalDrawScale: root.waveformDrawScale
 
                                     FadeBehavior on visible {
@@ -435,6 +438,7 @@ ApplicationWindow {
                             anchors.bottomMargin: root.waveformLanePadding
                             anchors.topMargin: root.waveformLanePadding
                             group: "[Channel1]"
+                            verticalDrawPosition: root.deckADrawPosition
                             verticalDrawScale: root.waveformDrawScale
                         }
                         }
@@ -456,6 +460,7 @@ ApplicationWindow {
                             anchors.bottomMargin: root.waveformLanePadding
                             anchors.topMargin: root.waveformLanePadding
                             group: "[Channel2]"
+                            verticalDrawPosition: root.deckBDrawPosition
                             verticalDrawScale: root.waveformDrawScale
                         }
                         Loader {
@@ -473,6 +478,7 @@ ApplicationWindow {
                             sourceComponent: Component {
                                 Skin.WaveformDisplay {
                                     group: deck4waveform.group
+                                    verticalDrawPosition: root.deckBDrawPosition
                                     verticalDrawScale: root.waveformDrawScale
 
                                     FadeBehavior on visible {
@@ -555,6 +561,8 @@ ApplicationWindow {
                     }
                 }
                 Rectangle {
+                    id: deckALabel
+
                     anchors.left: parent.left
                     anchors.leftMargin: 8
                     border.color: "#42A5F5"
@@ -563,7 +571,8 @@ ApplicationWindow {
                     height: 44
                     radius: 4
                     width: 38
-                    y: waveformStack.y + waveformContent.y + upperWaveformPane.y + deck1waveform.y + deck1waveform.height / 2 - height / 2
+                    y: waveformStack.y + waveformContent.y + upperWaveformPane.y
+                            + deck1waveform.y + deck1waveform.verticalDrawCenter - height / 2
                     z: 5
 
                     Text {
@@ -573,8 +582,36 @@ ApplicationWindow {
                         font.pixelSize: 24
                         text: "A"
                     }
+                    MouseArea {
+                        property real grabOffsetY: 0
+
+                        anchors.fill: parent
+                        enabled: root.editDeck
+                        preventStealing: true
+
+                        function updateDrawPosition(pointer) {
+                            const mapped = mapToItem(deck1waveform, pointer.x, pointer.y);
+                            const drawHeight = deck1waveform.height * root.waveformDrawScale;
+                            const travel = Math.max(0, deck1waveform.height - drawHeight);
+                            const desiredCenter = mapped.y - grabOffsetY;
+                            root.deckADrawPosition = travel > 0
+                                    ? Math.max(0, Math.min(1, (desiredCenter - drawHeight / 2) / travel))
+                                    : 0.5;
+                        }
+
+                        onPositionChanged: pointer => {
+                            if (pressed)
+                                updateDrawPosition(pointer);
+                        }
+                        onPressed: pointer => {
+                            const mapped = mapToItem(deck1waveform, pointer.x, pointer.y);
+                            grabOffsetY = mapped.y - deck1waveform.verticalDrawCenter;
+                        }
+                    }
                 }
                 Rectangle {
+                    id: deckBLabel
+
                     anchors.left: parent.left
                     anchors.leftMargin: 8
                     border.color: "#42A5F5"
@@ -583,7 +620,8 @@ ApplicationWindow {
                     height: 44
                     radius: 4
                     width: 38
-                    y: waveformStack.y + waveformContent.y + lowerWaveformPane.y + deck2waveform.y + deck2waveform.height / 2 - height / 2
+                    y: waveformStack.y + waveformContent.y + lowerWaveformPane.y
+                            + deck2waveform.y + deck2waveform.verticalDrawCenter - height / 2
                     z: 5
 
                     Text {
@@ -593,6 +631,32 @@ ApplicationWindow {
                         font.pixelSize: 24
                         text: "B"
                     }
+                    MouseArea {
+                        property real grabOffsetY: 0
+
+                        anchors.fill: parent
+                        enabled: root.editDeck
+                        preventStealing: true
+
+                        function updateDrawPosition(pointer) {
+                            const mapped = mapToItem(deck2waveform, pointer.x, pointer.y);
+                            const drawHeight = deck2waveform.height * root.waveformDrawScale;
+                            const travel = Math.max(0, deck2waveform.height - drawHeight);
+                            const desiredCenter = mapped.y - grabOffsetY;
+                            root.deckBDrawPosition = travel > 0
+                                    ? Math.max(0, Math.min(1, (desiredCenter - drawHeight / 2) / travel))
+                                    : 0.5;
+                        }
+
+                        onPositionChanged: pointer => {
+                            if (pressed)
+                                updateDrawPosition(pointer);
+                        }
+                        onPressed: pointer => {
+                            const mapped = mapToItem(deck2waveform, pointer.x, pointer.y);
+                            grabOffsetY = mapped.y - deck2waveform.verticalDrawCenter;
+                        }
+                    }
                 }
                 Rectangle {
                     width: 125
