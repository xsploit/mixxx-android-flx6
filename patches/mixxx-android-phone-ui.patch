diff --git a/packaging/android/AndroidManifest.xml b/packaging/android/AndroidManifest.xml
index c13b996..b73d951 100644
--- a/packaging/android/AndroidManifest.xml
+++ b/packaging/android/AndroidManifest.xml
@@ -2,8 +2,8 @@
 <manifest xmlns:android="http://schemas.android.com/apk/res/android"
     package="org.mixxx"
     android:installLocation="auto"
-    android:versionCode="1"
-    android:versionName="1.0">
+    android:versionCode="2"
+    android:versionName="0.2.0-phone-preview">
     <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"  />
     <uses-permission android:name="android.permission.INTERNET"  />
     <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>
@@ -22,7 +22,7 @@
         android:name="org.qtproject.qt.android.bindings.QtApplication"
         android:hardwareAccelerated="true"
         android:icon="@mipmap/ic_launcher"
-        android:label="Mixxx"
+        android:label="Mixxx FLX6 Preview"
         android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
         android:requestLegacyExternalStorage="true"
         android:allowBackup="true"
diff --git a/res/skins/LateNightQML/Toolbar/Toolbar.qml b/res/skins/LateNightQML/Toolbar/Toolbar.qml
index b5eb1e7..d9a0511 100644
--- a/res/skins/LateNightQML/Toolbar/Toolbar.qml
+++ b/res/skins/LateNightQML/Toolbar/Toolbar.qml
@@ -16,6 +16,7 @@ Rectangle {
         Full = 2
     }
 
+    property bool compactMode: false
     property bool editDeck: false
     property alias maximizeLibrary: maximizeLibraryButton.checked
     readonly property bool show4decks: show4DecksButton.checked && show4DecksButton.visible
@@ -43,22 +44,22 @@ Rectangle {
         return displayHour.toString() + ":" + date.getMinutes().toString().padStart(2, "0") + " " + suffix;
     }
     function initializeToolbarDefaults() {
-        if (!toolbarDefaultsControl.initialized || toolbarDefaultsControl.value >= 1.0) {
+        if (!toolbarDefaultsControl.initialized || toolbarDefaultsControl.value >= 2.0) {
             return;
         }
-        setControlValueIfInitialized(showMixerControl, 1.0);
+        setControlValueIfInitialized(showMixerControl, 0.0);
         setControlValueIfInitialized(showWaveformsControl, 1.0);
-        showEffectRackControl.value = 1.0;
+        showEffectRackControl.value = 0.0;
         showMicAuxControl.value = 0.0;
         showSamplersControl.value = 0.0;
         setControlValueIfInitialized(show4DecksControl, 0.0);
         showMaximizedLibraryControl.value = 0.0;
-        maxLibraryDecksControl.value = 1.0;
-        deckSizeControl.value = Toolbar.Compact;
+        maxLibraryDecksControl.value = 0.0;
+        deckSizeControl.value = Toolbar.Mini;
         showPreviewDecksControl.value = 0.0;
-        showLibraryCoverArtControl.value = 1.0;
+        showLibraryCoverArtControl.value = 0.0;
         samplerRowsControl.value = 1.0;
-        toolbarDefaultsControl.value = 1.0;
+        toolbarDefaultsControl.value = 2.0;
     }
     function closeSettingsPopups() {
         appMenuPopup.close();
@@ -592,8 +593,8 @@ Rectangle {
             LateNightToolbarButton {
                 id: maximizeLibraryButton
 
-                buttonWidth: 80
-                text: "BIG LIBRARY"
+                buttonWidth: root.compactMode ? 62 : 80
+                text: checked && root.compactMode ? "BACK" : (root.compactMode ? "LIBRARY" : "BIG LIBRARY")
 
                 onActivated: {
                     showMaximizedLibraryControl.value = checked ? 1.0 : 0.0;
@@ -608,6 +609,18 @@ Rectangle {
                 }
             }
         }
+        LateNightToolbarButton {
+            id: preferencesButton
+
+            buttonWidth: 66
+            text: "SETTINGS"
+            visible: root.compactMode
+
+            onActivated: {
+                checked = false;
+                Mixxx.PreferencesDialog.show();
+            }
+        }
         RowLayout {
             Layout.alignment: Qt.AlignVCenter
             spacing: 4
@@ -620,7 +633,7 @@ Rectangle {
                 LateNightToolbarButton {
                     id: showWaveformsButton
 
-                    buttonWidth: 80
+                    buttonWidth: root.compactMode ? 68 : 80
                     text: "WAVEFORMS"
 
                     onActivated: {
@@ -639,7 +652,7 @@ Rectangle {
             RowLayout {
                 Layout.alignment: Qt.AlignVCenter
                 spacing: -2
-                visible: root.show4decksAvailable
+                visible: root.show4decksAvailable && !root.compactMode
 
                 LateNightToolbarButton {
                     id: show4DecksButton
@@ -663,6 +676,7 @@ Rectangle {
             RowLayout {
                 Layout.alignment: Qt.AlignVCenter
                 spacing: -2
+                visible: !root.compactMode
 
                 LateNightToolbarButton {
                     id: showMixerButton
@@ -686,6 +700,7 @@ Rectangle {
             RowLayout {
                 Layout.alignment: Qt.AlignVCenter
                 spacing: -2
+                visible: !root.compactMode
 
                 LateNightToolbarButton {
                     id: showEffectsButton
@@ -709,6 +724,7 @@ Rectangle {
             RowLayout {
                 Layout.alignment: Qt.AlignVCenter
                 spacing: -2
+                visible: !root.compactMode
 
                 LateNightToolbarButton {
                     id: showSamplersButton
@@ -732,6 +748,7 @@ Rectangle {
             RowLayout {
                 Layout.alignment: Qt.AlignVCenter
                 spacing: -2
+                visible: !root.compactMode
 
                 LateNightToolbarButton {
                     id: showMicAuxButton
@@ -750,6 +767,27 @@ Rectangle {
             spacing: 4
             visible: maximizeLibraryButton.checked
 
+            LateNightToolbarButton {
+                buttonWidth: 54
+                text: "LOAD 1"
+                visible: root.compactMode
+
+                onActivated: {
+                    checked = false;
+                    loadSelectedTrackDeck1Control.trigger();
+                }
+            }
+            LateNightToolbarButton {
+                buttonWidth: 54
+                text: "LOAD 2"
+                visible: root.compactMode
+
+                onActivated: {
+                    checked = false;
+                    loadSelectedTrackDeck2Control.trigger();
+                }
+            }
+
             RowLayout {
                 Layout.alignment: Qt.AlignVCenter
                 spacing: -2
@@ -776,7 +814,7 @@ Rectangle {
             RowLayout {
                 Layout.alignment: Qt.AlignVCenter
                 spacing: -2
-                visible: root.show4decksAvailable
+                visible: root.show4decksAvailable && !root.compactMode
 
                 LateNightToolbarButton {
                     id: maximizedShow4DecksButton
@@ -797,14 +835,17 @@ Rectangle {
         }
         AutoDjIndicator {
             active: autoDjControl.value > 0
+            visible: !root.compactMode && active
         }
         ClockIndicator {
             id: clockLabel
+            visible: !root.compactMode
         }
         Item {
             Layout.preferredWidth: 7
         }
         LatencyIndicator {
+            visible: !root.compactMode
             overload: latencyOverloadControl.value > 0
             overloadOpacity: pulseDriver.opacity
             usage: latencyUsageControl.initialized ? latencyUsageControl.value : 0.0
@@ -819,6 +860,7 @@ Rectangle {
             Layout.preferredWidth: 9
         }
         RecordingIndicator {
+            visible: !root.compactMode
             durationText: Mixxx.Recording.durationText
             pulseOpacity: pulseDriver.opacity
             status: recordingStatusControl.value
@@ -828,6 +870,7 @@ Rectangle {
             }
         }
         BroadcastIndicator {
+            visible: !root.compactMode
             pulseOpacity: pulseDriver.opacity
             status: broadcastStatusControl.value
 
@@ -841,6 +884,7 @@ Rectangle {
             Layout.preferredWidth: 74
             fillMode: Image.PreserveAspectFit
             source: LateNightTheme.lateNightAsset("style", "mixxx_logo_small.svg")
+            visible: !root.compactMode
         }
     }
     ToolbarSettingsPopup {
diff --git a/res/skins/LateNightQML/main.qml b/res/skins/LateNightQML/main.qml
index c731ee4..d028b01 100644
--- a/res/skins/LateNightQML/main.qml
+++ b/res/skins/LateNightQML/main.qml
@@ -14,7 +14,8 @@ ApplicationWindow {
     property alias editDeck: toolbar.editDeck
     property var focusedDeck: null
     property alias maximizeLibrary: toolbar.maximizeLibrary
-    readonly property int fullDeckHeight: 206
+    readonly property bool compactScreen: width < 1100 || height < 600
+    readonly property int fullDeckHeight: compactScreen ? 80 : 206
     readonly property int minimizedDeckHeight: 80
     readonly property int numDecks: 4
     readonly property int numSamplers: 64
@@ -26,11 +27,11 @@ ApplicationWindow {
     readonly property bool showWaveforms: toolbar.showWaveforms
 
     color: LateNightTheme.backgroundColor
-    height: 1008
-    minimumHeight: 668
-    minimumWidth: 1280
+    height: 450
+    minimumHeight: 320
+    minimumWidth: 640
     visible: true
-    width: 1792
+    width: 800
 
     Mixxx.ControlProxy {
         group: "[App]"
@@ -188,6 +189,7 @@ ApplicationWindow {
         LateNightToolbar.Toolbar {
             id: toolbar
 
+            compactMode: root.compactScreen
             show4decksAvailable: root.height > 515
             width: parent.width
         }
@@ -235,7 +237,7 @@ ApplicationWindow {
                 id: waveforms
 
                 SplitView.fillHeight: !library.active
-                SplitView.preferredHeight: library.active ? 120 : undefined
+                SplitView.preferredHeight: library.active ? (root.compactScreen ? Math.min(180, root.height * 0.38) : 120) : undefined
                 visible: root.showWaveforms && !root.maximizeLibrary
                 show4decks: root.show4decks
 
@@ -247,8 +249,8 @@ ApplicationWindow {
                 id: deckPane
 
                 SplitView.fillHeight: library.active
-                SplitView.maximumHeight: library.active ? undefined : mixer.height
-                SplitView.minimumHeight: mixer.height
+                SplitView.maximumHeight: library.active || root.compactScreen ? undefined : mixer.height
+                SplitView.minimumHeight: root.compactScreen ? 150 : mixer.height
                 width: splitView.width
 
                 LateNightDeck.Deck {
@@ -257,7 +259,7 @@ ApplicationWindow {
                     editMode: root.editDeck
                     group: "[Channel1]"
                     height: root.maximizeLibrary ? (root.showMaximizedDecks ? root.minimizedDeckHeight : 0) : root.fullDeckHeight
-                    minimized: root.maximizeLibrary
+                    minimized: root.compactScreen || root.maximizeLibrary
                     visible: !root.maximizeLibrary || root.showMaximizedDecks
 
                     Behavior on height {
@@ -382,7 +384,7 @@ ApplicationWindow {
                     editMode: root.editDeck
                     group: "[Channel2]"
                     height: root.maximizeLibrary ? (root.showMaximizedDecks ? root.minimizedDeckHeight : 0) : root.fullDeckHeight
-                    minimized: root.maximizeLibrary
+                    minimized: root.compactScreen || root.maximizeLibrary
                     visible: !root.maximizeLibrary || root.showMaximizedDecks
 
                     Behavior on height {
@@ -525,7 +527,7 @@ ApplicationWindow {
                 Loader {
                     id: library
 
-                    active: root.maximizeLibrary || root.height - mixer.height >= 400
+                    active: root.compactScreen || root.maximizeLibrary || root.height - mixer.height >= 400
                     width: parent.width
 
                     sourceComponent: Component {
@@ -559,7 +561,15 @@ ApplicationWindow {
                             }
                         },
                         State {
-                            when: !root.maximizeLibrary && root.height - mixer.height < 400
+                            when: root.compactScreen && !root.maximizeLibrary
+
+                            AnchorChanges {
+                                anchors.top: deck1.bottom
+                                target: library
+                            }
+                        },
+                        State {
+                            when: !root.compactScreen && !root.maximizeLibrary && root.height - mixer.height < 400
 
                             PropertyChanges {
                                 target: library
diff --git a/res/skins/LateNightQML/skin.ini b/res/skins/LateNightQML/skin.ini
index 8fb65de..1b5daad 100644
--- a/res/skins/LateNightQML/skin.ini
+++ b/res/skins/LateNightQML/skin.ini
@@ -1,6 +1,6 @@
 [Skin]
 name = LateNight QML (Experimental)
 description = QML-based LateNight skin with legacy library integration (Developer Preview)
-min_pixel_width = 1280
-min_pixel_height = 720
+min_pixel_width = 640
+min_pixel_height = 320
 color_schemes = PaleMoon, Classic
diff --git a/src/qml/qmlapplication.cpp b/src/qml/qmlapplication.cpp
index b91141c..5e6d858 100644
--- a/src/qml/qmlapplication.cpp
+++ b/src/qml/qmlapplication.cpp
@@ -212,7 +212,11 @@ QmlApplication::QmlApplication(
     connect(&m_guiTickTimer, &QTimer::timeout, this, [this]() {
         m_pGuiTick->process();
     });
+#if defined(Q_OS_ANDROID)
+    m_guiTickTimer.start(std::chrono::milliseconds(33));
+#else
     m_guiTickTimer.start(std::chrono::milliseconds(16));
+#endif
 
     m_loadSucceeded = loadQml(m_mainFilePath);
     if (!m_loadSucceeded) {
@@ -237,7 +241,7 @@ QmlApplication::QmlApplication(
         return;
     }
     int32_t thread32 = gettid();
-    m_perfSession = APerformanceHint_createSession(manager, &thread32, 1, 1e9 / 60);
+    m_perfSession = APerformanceHint_createSession(manager, &thread32, 1, 1e9 / 30);
     VERIFY_OR_DEBUG_ASSERT(m_perfSession) {
         __android_log_print(ANDROID_LOG_WARN, "mixxx", "unable to create a ADPF session!");
     }
diff --git a/src/qml/qmldlgpreferencesproxy.cpp b/src/qml/qmldlgpreferencesproxy.cpp
index 7c67872..054bb13 100644
--- a/src/qml/qmldlgpreferencesproxy.cpp
+++ b/src/qml/qmldlgpreferencesproxy.cpp
@@ -14,7 +14,13 @@ QmlDlgPreferencesProxy::QmlDlgPreferencesProxy(
 }
 
 void QmlDlgPreferencesProxy::show() {
+#if defined(Q_OS_ANDROID)
+    m_pDlgPreferences->showMaximized();
+#else
     m_pDlgPreferences->show();
+#endif
+    m_pDlgPreferences->raise();
+    m_pDlgPreferences->activateWindow();
 }
 
 // static
